spring:
  datasource:
    url: jdbc:h2:mem:ordermanagement_test
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    database-platform: org.hibernate.dialect.H2Dialect

eureka:
  client:
    enabled: false

app:
  product-catalog:
    url: http://localhost:8081
